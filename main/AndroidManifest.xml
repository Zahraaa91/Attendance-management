<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancemanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/un"
        android:label="@string/attendance_management_system"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Warnings2Activity"></activity>
        <activity android:name=".Warnings3Activity" />
        <activity android:name=".FirstwarningActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".ForthActivity" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>